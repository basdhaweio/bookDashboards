# Notes / open questions

Running list of things to verify against the source sheets. `data/bookdb.json`
is an export, so fixes land in the Google Sheet first and arrive here on the
next bundle.

## To check

- **Hunger Games — second prequel ownership.** Verify whether *Sunrise on the
  Reaping* (Suzanne Collins, Hunger Games Prequels #2) is actually owned. The
  bundle as of `2026-08-01` has it marked `Owned = x`, Media `Print`, owner
  `butthead` (Butthead Individual), and the Butthead Series row "Hunger Games
  Prequels" reads Count 2 / Owned 2 — so the series count depends on this being
  right. Not marked read. If it isn't on the shelf, clear `Owned` on the
  individual row and drop the series Owned to 1.
